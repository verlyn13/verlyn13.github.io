# Phase 10.5 — Finding Smoke-Test (verlyn13.github.io)

**Audit date:** 2026-05-21
**Snapshot ref:** `ee6c7e55331faaf55fa6bb4cde49abd9e1c96f9a`
**Current ref:** `ee6c7e55331faaf55fa6bb4cde49abd9e1c96f9a` (identical — audit conducted on same revision the profile snapshots)
**Spec anchor:** agentic-audit-spec-v3.1 §7 Phase 10.5

## 0. Method

Re-grep / re-read every cited evidence path against current HEAD. Because the audit was conducted on the same revision the profile snapshots (commits already landed within the profile cycle per `meta.evidence_extensions[0]`), the expected outcome distribution was high `confirmed-current`, near-zero `struck`/`drift-only` from re-evidencing.

For each fitness-function candidate: verify enforcement tech currently installed or installable in the project's CI per §10.5 step 2.

---

## 1. Outcome distribution (12 findings)

| Outcome | Count | Findings |
|---|---:|---|
| `confirmed-current` | 9 | F-001, F-002, F-003, F-006, F-007, F-008, F-009, F-010, F-012 |
| `re-evidenced` | 1 | F-004 (line-number tolerance ±2 on README citations) |
| `struck` | 1 | F-005 (resolved-in-profile per evidence_extensions[0]) |
| `drift-only` | 1 | F-011 (declared-vs-enforced caveat) |
| `reclassified` | 0 | — |
| `promoted` | 0 | — |
| `demoted` | 0 | — |
| `not-run` | 0 | — |

**Stale-evidence rate: 16.7% (2 of 12 are struck or drift-only).**

**Halt threshold per §4.1:** more than 1/3 (33.3%) struck or drift-only triggers halt. **NOT exceeded.** Audit ships without halt.

---

## 2. Per-finding outcomes

### F-001 — MAAT framework narrative dominance — `confirmed-current`

Re-verified at HEAD `ee6c7e5`:
- 12 favicon refs: `grep -rl 'maat_feather_16.svg' --include='*.html' .` returns 12 files (11 deployed + 1 experiments).
- `index.html` L44 / L76 / L117: lead-para MAAT + CS-theory MAAT + nav card href all present.
- `cv.html` L83 + `projects/maat.html` L6 title + L48 h1: all confirmed.
- README.md L11: "MAAT Framework" first in 2-project list.

All 20+ surface instances confirmed. Pair B with sibling F-002 stands.

### F-002 — DEV_GUIDE.md vs AGENTS.md contradiction — `confirmed-current`

- AGENTS.md L116: "Rem-based scale via `--space-1` through `--space-5`" confirmed.
- DEV_GUIDE.md L15-18: "8-point grid system…`--space-xs` (4px) through `--space-2xl`" confirmed.
- `assets/styles.css` (DEV_GUIDE L52 claim): absent.
- `research/maat.html` + `research/scopecam.html` + `academic/` tree: all absent.

Minor re-evidencing note: AGENTS.md typography line is at L118 (Phase 8 cited L117) — 1-line tolerance, substance unchanged.

### F-003 — Biome 2 rules at `warn` — `confirmed-current`

`biome.json` L15-20: `complexity.noImportantStyles: "warn"` + `style.noDescendingSpecificity: "warn"` both confirmed. `deploy.yml` L36-37 runs `npx biome check .` with no `--error-on-warnings` flag.

### F-004 — README.md stale MIT © 2025 — `re-evidenced`

Substance unchanged; line numbers shifted by 2:

| Original citation | Re-grep result |
|---|---|
| `README.md:13` | `README.md:15` ("MIT © 2025 Jeffrey Johnson") |
| `README.md:10-11` | `README.md:11-12` (2-project list with MAAT first) |

Updated per §10.5 step 4 (re-evidenced; citations updated; substance preserved).

### F-005 — Working-tree drift — `struck`

Already classified `struck` at Phase 10 draft time (resolved-in-profile per `meta.evidence_extensions[0]`). Re-verification confirms working tree clean at HEAD `ee6c7e5`. Finding retained in this smoke-test log; absent from final priority ranking.

### F-006 — PostToolUse hook silent-error swallow — `confirmed-current`

`.claude/settings.json` L27-37 PostToolUse hook re-grep confirms full command string includes `npx biome format --write "$file_path" 2>/dev/null; esac; exit 0` with 10-second timeout. All flags from Phase 6 A-04 + Phase 7 §3.4 stand.

### F-007 — experiments-sandbox bounded-context decision — `confirmed-current`

`experiments/` filesystem matches Phase 2 §2.4 inventory: 9 top-level entries (3 HTML prototypes + svg-src/ subdir + themes/academic.css + braid-timeline.svg + 2 mono icons + timeline-data.json). vite.config.js entry list excludes all experiments paths.

### F-008 — Cross-repo content-pipeline consumer side — `confirmed-current`

`grep -rn "source: new-direction-2026\|<!-- source" --include='*.html' .` returns zero matches. No source-revision provenance comment in any HTML file. Pair A consumer-side gap confirmed.

### F-009 — context7 MCP uninventoried — `confirmed-current`

`ls -la .mcp.json` returns "No such file or directory". CLAUDE.md L27-33 MCP table confirmed (context7 + resolve-library-id + query-docs declared).

### F-010 — 5 GitHub Actions @v* not @SHA — `confirmed-current`

`grep -n -E "uses:.*@v" .github/workflows/deploy.yml` returns 7 lines (L25, L28, L48, L51, L64, L68, L83) — 5 distinct actions (checkout + setup-node duplicated across quality + build jobs). All @major-version, none @SHA. `actions/deploy-pages@v4` at L83 confirmed as the HIGH-risk publish action.

### F-011 — PR-time CI declared-vs-enforced gap — `drift-only`

`.github/workflows/deploy.yml` L63 + L67 + L74: 3 instances of `if: github.event_name != 'pull_request'` confirmed. **External GitHub branch-protection state not observable from Phase 10.5 re-grep** (requires admin token). Outcome `drift-only` per §10.5 step 4: discrepancy between profile claim (declared CI gate) and external enforcement state is itself the finding. Carried to SUMMARY caveat.

### F-012 — THEMES.md overstated capability — `confirmed-current`

- THEMES.md L1-5: capability claim confirmed.
- THEMES.md L25: `styles-academic.css` filename claim confirmed.
- `assets/styles-academic.css`: absent.
- `experiments/themes/academic.css`: present (different bounded context, not at claimed path).

---

## 3. Fitness-function enforceability check (§10.5 step 2)

For each FF: enforcement tech installed or installable in project's CI?

| FF | Enforceable | Note |
|---|:---:|---|
| FF-001 claude-code-hook-observability | ✓ | jq + grep + tee — CI step addable to mise pipeline |
| FF-002 claude-code-hook-scope-declaration | ✓ (partial) | Claude Code hook schema lacks native filesystem_scope/approval_mode fields; parallel manifest + JSON-schema validator is implementable |
| FF-003 pin-github-actions-to-sha | ✓ | grep with negative-pattern match; dependabot can auto-update |
| FF-004 per-job-actions-permissions | ✓ | actionlint + yq; well-established |
| FF-005 maat-positioning-narrative-balance | **manual-review-only** | No operator-declared current-positioning contract at HEAD; demoted at Phase 10 draft time |
| FF-006 html-source-revision-provenance-comment | ✓ | Vite `transformIndexHtml` hook + CI grep; cooperative with sibling FF-006 |
| FF-007 docs-cross-reference-check | ✓ | ripgrep + Python pathlib; planning-summer-2026's `tools/lint_docs.py` is the reference impl |
| FF-008 branch-protection-declaration | **manual-review-only** | Full automation requires admin-token GitHub-API export; classified at draft time |
| FF-009 biome-rule-severity-progression | ✓ | grep on biome.json |
| FF-010 copyright-year-check | ✓ | grep + `date +%Y` |
| FF-011 mcp-project-scoping | ✓ | Python script + cross-check; same enforcement as sibling NDM FF-007 |
| FF-012 mcp-tool-schema-recorded | ✓ | MCP `tools/list` snapshot to `.mcp-schemas/` |

**Result:** 10 of 12 FFs are CI-implementable. 2 FFs (FF-005, FF-008) were already classified `manual-review-only` at Phase 10 draft time — no demotion required in smoke-test. **Zero further demotions needed.**

---

## 4. Exit check (§7 Phase 10.5)

| Criterion | Status |
|---|---|
| Every finding has a smoke-test outcome | ✓ 12/12 |
| Every struck finding is absent from final priority ranking but retained in smoke-test log | ✓ (F-005 absent from 10-findings.md §3 priority table) |
| Every drift-only item is recorded as profile/code drift | ✓ (F-011 carried to SUMMARY caveat per Phase 0 routing) |
| If >1/3 of findings receive `struck` or `drift-only`, halt per §4.1 | NOT TRIGGERED (16.7% << 33.3%) |
| Smoke-test could not be performed (`not-run`)? Reason cited? | n/a (0 not-run) |
| Snapshot evidence preserved (not erased) | ✓ (10-findings.json snapshot_evidence intact) |
| Re-evidencing recorded (not silent line-number updates) | ✓ (F-002 minor 1-line tolerance + F-004 re-evidenced with explicit line-shift table) |

---

## 5. Final outputs after smoke-test

- **Final active findings:** 10 (F-001, F-002, F-003, F-004, F-006, F-007, F-008, F-009, F-010, F-012)
- **Struck (retained in log, absent from priority):** 1 (F-005)
- **Drift-only (recorded as drift):** 1 (F-011)
- **Total drafted:** 12

Priority ranking unchanged from `10-findings.md §3`.

See [`10-findings.md`](10-findings.md), [`10-findings.json`](10-findings.json), [`10-fitness-functions.md`](10-fitness-functions.md), [`10-fitness-functions.json`](10-fitness-functions.json), [`10-scores.json`](10-scores.json).
